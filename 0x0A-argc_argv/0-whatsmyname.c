#include "holberton.h"
/**
 * main - print name.
 * @argc: number of parameters.
 * @argv: array of parameters.
 * Return: 0 always.
 */
int main(int argc, char *argv[])
{
	if (argc)
	{
	}
	printf("%s\n", argv[0]);

	return (0);
}
